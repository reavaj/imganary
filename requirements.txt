# PyTorch (requires ARM Python 3.12 on Apple Silicon)
torch>=2.4

# HTTP client (for Ollama API)
httpx>=0.27.0

# Configuration
pydantic>=2.0
pydantic-settings>=2.0

# YOLO object detection
ultralytics>=8.0

# CLIP semantic understanding
transformers>=4.36.0
Pillow>=10.0
sentencepiece>=0.1.99

# Gemini API (prompt expansion)
google-genai>=1.0.0

# Web image search (style auto-research)
ddgs>=7.0

# FLUX.1 image generation (MLX-native, Apple Silicon)
mflux>=0.9.0
