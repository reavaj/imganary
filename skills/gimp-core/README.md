# GIMP Core Skills

CLI and Python-Fu wrappers for common GIMP operations:

- Resize and crop
- Color adjustments (levels, curves, saturation)
- Filters (blur, sharpen, noise reduction)
- Format conversion
- Batch processing across image folders

Each skill is self-contained and generates reusable Python-Fu scripts saved to `scripts/`.
